<!-- Page header -->
<div class="page-header page-header-light">
    <div class="breadcrumb-line breadcrumb-line-light header-elements-md-inline">
        <div class="d-flex">
            <div class="breadcrumb">
                <a href="/" class="breadcrumb-item"><i class="icon-home2 mr-2"></i> Home</a>
                <a href="/event" class="breadcrumb-item"> Event</a>
                <span class="breadcrumb-item active">Edit</span>
            </div>
            <a href="#" class="header-elements-toggle text-default d-md-none"><i class="icon-more"></i></a>
        </div>
    </div>
</div>
<!-- /page header -->

<!-- Content area -->
<div class="content">
    <!-- Vertical form options -->
    <div class="row">
        <div class="col-md-6">

            <!-- Basic layout-->
            <div class="card">
                <div class="card-header header-elements-inline">
                    <h5 class="card-title">Edit</h5>
                </div>

                <% if (messages.error) { %>
                    <div style="margin: 0 20px 0 20px" class="alert alert-danger alert-styled-left alert-dismissible">
                        <span class="font-weight-semibold">Error!</span> <%- messages.error %>
                    </div>
                <% } %>

                <% if (messages.success) { %>
                    <div style="margin: 0 20px 0 20px" class="alert alert-success alert-styled-left alert-arrow-left alert-dismissible">
                        <span class="font-weight-semibold">Success!</span> <%- messages.success %>
                    </div>
                <% } %>

                <div class="card-body">
                    <form action="/event/edit/<%=id%>" method="post" enctype="multipart/form-data" name="form1">
                        <input type="hidden" name="id" value="<%= id %>" />
                        <div class="form-group">
                            <label>Event Title: </label>
                            <input type="text" name="event_title" value="<%= event_title %>" id="event_title" class="form-control" placeholder="Event Title">
                        </div>
                        <div class="form-group">
                            <label>Event Venue: </label>
                            <input type="text" name="event_venue" value="<%= event_venue %>" id="event_venue" class="form-control" placeholder="Event Venue">
                        </div>
                        <div class="form-group">
                            <label>Event Details: </label>
                            <textarea id="my-expressjs-tinymce-app" name="event_details"><%= event_details %></textarea>
                        </div>
                        <div class="form-group">
                            <label>Event Short Details: </label>
                            <textarea id="my-expressjs-tinymce-app" name="event_short_details"><%= event_short_details %></textarea>
                        </div>
                        <div class="form-group">
                            <label>Event Date Time:</label> <label style="font-weight: bold"><%=moment(event_date).format('LLL')%></label>
                            <input class="form-control" value="<%= event_date %>" type="datetime-local" name="event_date">
                        </div>

                        <div class="form-group">
                            <label>Cover Image: </label>
                            <div>
                                <%if(cover_image===""){%>
                                    Previous Image not available
                                <%}else{%>
                                <img width="100%" src="/cover_image_event/<%=cover_image%>"/>
                                <%}%>
                            </div>
                            <input name="_image" type="file" class="form-input-styled" accept="image/*" data-fouc>
                            <span class="form-text text-muted">Accepted formats: png, jpg. Max file size 3Mb</span>
                        </div>

                        <div class="form-group">
                            <label>Event Session: </label>
                            <select name="event_session" class="form-control select-fixed-single" data-fouc>
                                <option value="Current Event" <%= event_session === "Current Event" ? 'selected' : ''%>>Current Event</option>
                                <option value="Past Event" <%= event_session === "Past Event" ? 'selected' : ''%>>Past Event</option>
                                <option value="Upcoming Event" <%= event_session === "Upcoming Event" ? 'selected' : ''%>>Upcoming Event</option>
                            </select>
                        </div>

<!--                        <div class="form-group">-->
<!--                            <label>Event registration fees: </label>-->
<!--                            <input type="text" name="event_fees" value="<%= event_fees %>" id="event_fees" class="form-control" placeholder="Event registration fees">-->
<!--                        </div>-->

                        <div class="form-group">
                            <label>Membership renew fees: </label>
                            <input type="text" name="membership_renew_fees" value="<%= membership_renew_fees %>" id="membership_renew_fees" class="form-control" placeholder="Membership Renew Fees">
                        </div>
                        <div class="form-group">
                            <label>Member single fees: </label>
                            <input type="text" name="member_single_fees" value="<%= member_single_fees %>" id="member_single_fees" class="form-control" placeholder="Member single fees">
                        </div>
                        <div class="form-group">
                            <label>Member spouse fees: </label>
                            <input type="text" name="member_spouse_fees" value="<%= member_spouse_fees %>" id="member_spouse_fees" class="form-control" placeholder="Member spouse fees">
                        </div>
                        <div class="form-group">
                            <label>Student/Guest single fees: </label>
                            <input type="text" name="student_single_fees" value="<%= student_single_fees %>" id="student_single_fees" class="form-control" placeholder="Student/Guest single fees">
                        </div>
                        <div class="form-group">
                            <label>Student/Guest spouse fees: </label>
                            <input type="text" name="student_spouse_fees" value="<%= student_spouse_fees %>" id="student_spouse_fees" class="form-control" placeholder="Student/Guest spouse fees">
                        </div>

                        <div class="form-group">
                            <label>Status: </label>
                            <select name="status" class="form-control select-fixed-single" data-fouc>
                                <% if (status == '0'){%>
                                    <option value="1">Published</option>
                                    <option value="0" selected>Unpublished</option>
                                <%}else if(status == '1'){%>
                                    <option value="1" selected>Published</option>
                                    <option value="0">Unpublished</option>
                                <%}else{%>
                                    <option value="1" selected>Published</option>
                                    <option value="0">Unpublished</option>
                                <%}%>
                            </select>
                        </div>

                        <div class="text-right">
                            <button type="submit" class="btn btn-primary">Update
                                <i class="icon-paperplane ml-2"></i>
                            </button>
                        </div>
                    </form>
                </div>
            </div>
            <!-- /basic layout -->

        </div>
    </div>
    <!-- /vertical form options -->
</div>
<!-- /content area -->
